spring:
  ai:
    azure:
      openai:
        api-key: KEY
        endpoint: ENDPOINT
        chat:
          options:
            deployment-name: gpt-4o
        image:
          options:
            deployment-name: dall-e-3
